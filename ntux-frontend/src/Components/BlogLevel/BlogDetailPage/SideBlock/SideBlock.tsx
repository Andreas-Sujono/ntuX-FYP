import React, { memo } from 'react';
import { Container } from './Styles';

function SideBlock(): React.ReactElement {
  return (
    <Container>
      <div>sdsd</div>
    </Container>
  );
}

export default memo(SideBlock);
