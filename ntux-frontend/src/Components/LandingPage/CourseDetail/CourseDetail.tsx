import React, { memo } from 'react';
import { Container } from './Styles';

function CourseDetail(): React.ReactElement {
  return <Container>Course Detail</Container>;
}

export default memo(CourseDetail);
