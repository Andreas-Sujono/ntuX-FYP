import React, { memo } from 'react';
import { Container } from './Styles';

function RegisterCourse(): React.ReactElement {
  return <Container>RegisterCourse</Container>;
}

export default memo(RegisterCourse);
